version: 2.1

jobs:
  build:
#     machine: true
      # image: ubuntu-2004:202010-01
    docker:
      - image: cimg/base:2021.04

    steps:
      - run:
          name: "Say hello"
          command: |
            ps aux
            whoami
            pwd
            cat /etc/hosts
